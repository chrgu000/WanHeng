<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dq.dao.BigTypeDao">
	<insert id="save">
		insert into
		dq_big_type(name,num)values(#{name},#{num})
	</insert>
	<update id="update">
		update dq_big_type
		<set>
			<if test="name!=null and name!=''">
				name=#{name},
</if>
			<if test="num!=null and num!=''">
				num=#{num},
</if>
		</set>
		where id=#{id}
	</update>
	<delete id="deleteByIds">
		delete from dq_big_type where id in
		<foreach collection="ids" open="(" close=")" separator=","
			item="id">#{id}</foreach>
	</delete>
	<select id="getById" resultType="BigType">
		select * from dq_big_type
		where
		id=#{id}
	</select>
	<select id="getAllByPage" resultType="BigType">
		select * from dq_big_type
		order by num desc limit #{begin},#{pageSize}
	</select>
	<select id="getRows" resultType="int">
		select count(*) from dq_big_type
	</select>
	<select id="getAllBigType" resultType="BigType">
	  select * from dq_big_type
	</select>
</mapper>